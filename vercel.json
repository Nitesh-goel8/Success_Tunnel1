{
  "framework": "nextjs",
  "installCommand": "if [ -d successtunnel_app ]; then cd successtunnel_app; fi; npm install",
  "buildCommand": "if [ -d successtunnel_app ]; then cd successtunnel_app; fi; npm run build"
}
